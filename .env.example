# ════════════════════════════════════════════════════════════════════════════
# VAHDAM Mailer Studio — Environment Variables
# ════════════════════════════════════════════════════════════════════════════
# This file documents the env vars the app needs. Copy to `.env.local` for
# local `vercel dev` use, OR add the same vars in the Vercel dashboard for
# Production / Preview / Development environments.
#
# NEVER commit a populated `.env.local` — it's already in `.gitignore`.
# This `.env.example` IS committed (no real values, just placeholders).
# ════════════════════════════════════════════════════════════════════════════

# ════════════════════════════════════════════════════════════════════════════
# AT LEAST ONE TEXT PROVIDER REQUIRED — provider waterfall is OpenAI → Gemini
# ════════════════════════════════════════════════════════════════════════════

# ─── OPTION A — Google Gemini (FREE, recommended if you have no budget) ───
# Get key:    https://aistudio.google.com/app/apikey  (free, just sign in with Google)
# Free tier:  1,500 requests/day · 1M tokens/day on gemini-1.5-flash
# Cost:       $0 indefinitely if you stay under the free tier
# Used by:    api/ai/generate.js as fallback when OPENAI_API_KEY is absent
GEMINI_API_KEY=
GEMINI_TEXT_MODEL=gemini-1.5-flash

# ─── OPTION B — OpenAI (paid, used FIRST if both keys are set) ───
# Get key:    https://platform.openai.com/api-keys
# Fund acct:  https://platform.openai.com/settings/organization/billing  ($5 min)
# Used by:    api/ai/generate.js + api/ai/image.js
OPENAI_API_KEY=
OPENAI_TEXT_MODEL=gpt-4o-mini
OPENAI_IMAGE_MODEL=gpt-image-1

# ─── IMAGE GENERATION ───
# If OPENAI_API_KEY is set: api/ai/image.js uses gpt-image-1 (paid)
# If not:                    api/ai/image.js falls back to Pollinations FLUX (free, no auth)
# Pollinations needs NO env var — works out of the box.

APP_AI_PROVIDER=openai

# ─── OPTIONAL — Supabase (cross-device dashboard storage) ───
# If unset, the dashboard falls back to localStorage-only.
# Get from:   https://supabase.com/dashboard/project/_/settings/api
# SUPABASE_URL=https://xxxxx.supabase.co
# SUPABASE_ANON_KEY=eyJhbGciOi...

# ─── INTERNAL — set by Vercel automatically; do NOT override ───
# VERCEL=1
# VERCEL_ENV=production|preview|development
# VERCEL_URL=...
