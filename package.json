{
  "name": "vahdam-mailer-architect",
  "version": "1.0.0",
  "private": true,
  "description": "VAHDAM Mailer Studio — static HTML SPA with Vercel serverless functions for OpenAI server-side calls.",
  "engines": {
    "node": ">=20.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}
